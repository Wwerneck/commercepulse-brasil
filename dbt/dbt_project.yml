name: commercepulse_brasil
version: "1.0.0"
config-version: 2

profile: commercepulse_brasil

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  commercepulse_brasil:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
